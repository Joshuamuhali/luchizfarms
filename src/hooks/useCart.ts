/**
 * DEPRECATED — do not use.
 * This hook keyed cart items by product name (string) which breaks with similar
 * product names and is disconnected from Supabase product IDs.
 *
 * Use usePersistedCart (keyed by UUID) instead — that's what OrderSection uses.
 */
export {};
